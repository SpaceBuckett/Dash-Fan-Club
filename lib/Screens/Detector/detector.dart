import 'package:flutter/material.dart';

class DashDetector extends StatelessWidget {
  const DashDetector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Image.asset('assets/dash.png'),
    );
  }
}
